import { KeyedElementDirective } from './keyed-element.directive';

describe('KeyedElementDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyedElementDirective();
    expect(directive).toBeTruthy();
  });
});
